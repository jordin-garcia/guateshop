"""TODO (Integrante 1): PayPalAdapter y BancoLocalPayAdapter.

Ambos deben implementar PaymentProcessor, recibir el servicio externo por
constructor (composición) y traducir procesar_pago() a la llamada heredada.
"""
